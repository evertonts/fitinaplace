<div style="float: right;margin-right: 55%;font-family: Trebuchet MS; color: #BD2026">
<h1 style="font-size: 20pt"><%= @user.name %></h1>
<p>Email: <%= @user.email if @user.email %></p>
<p>Telefone: <%= @user.telefone if @user.telefone %></p>
</div>
<%= image_tag @user.avatar.url(:medium)%>
<h3 style="font-family: Trebuchet MS; color: #BD2026; font-size: 20pt"> Meus interesses</h3>

<% if @negociations.empty? %>
	<p> Você não tem nenhuma negociação em andamento.
	 <%= link_to "Clique aqui", events_path %>  </a> para checar eventos. </p>
<% else %>
<table class="user">
  <tr>
    <th >Reservado</th>
    <th >Nome</th>
    <th >Descrição</th>
  </tr>

<% @negociations.each do |negociation| %>
	<% offering = Offering.find negociation.offering_id
	event = Event.find offering.event_id
	space = Resource.find offering.space_id %>
  <tr>
  	<% if negociation.status == true %>
    <td>Reservado</td>
    <% else %>
    <td>Aguardando</td>
    <%end%>
    <td><%= event.name %></td>
    <td><%= space.description %></td>
  </tr>
<% end %>

</table>

<% end %>
<h3 style="font-family: Trebuchet MS; color: #BD2026;font-size: 20pt"> Minhas ofertas: </h3>
<% if @offerings.empty? %>
	<p> Você não está ofertando nenhum lugar.
	 <%= link_to "Clique aqui", events_path %>  </a> para checar novos eventos e ofertar hospedagens. </p>
<% else %>
<table class="user">
	<tr>
		<th >Evento</th>
		<th >Espaço</th>
		<th >Interessados</th>
	</tr>
	<% for offering in  @offerings%>
	<tr>
	 <% event = Event.find offering.event_id%>
	 
	 <td> <%= event.name%> </td>
	 <% space = Resource.find offering.space_id%>
	 <td><%= space.description %></td>
	 <td> <%= link_to "Checar interessados", negociations_path(:offering_id => offering.id) %> </td>
	</tr>
<% end%>
<%end%>
</table>
<br />

